# AllowList

Small python script to update (add/remove) a file of allowed IP addresses

## Features

- Add one or more IP addresses
- Remove one or more IP addresses
- Prevent duplicate IP addresses
- Store IP addresses in `ip_addresses.txt`
- Display an error message for invalid menu choices

## Requirements

Install `colorama` before running the script:

```bash
pip install colorama
